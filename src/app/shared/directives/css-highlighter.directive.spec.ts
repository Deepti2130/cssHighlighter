import { CssHighlighterDirective } from './css-highlighter.directive';

describe('CssHighlighterDirective', () => {
  it('should create an instance', () => {
    const directive = new CssHighlighterDirective();
    expect(directive).toBeTruthy();
  });
});
